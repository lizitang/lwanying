### null和undefined区别
摘自阮一峰笔记  
```
1.null表示没对象，即此处不应该有值。典型用法是：
    (1)作为函数的参数，表示该函数的参数不是对象
    (2)作为对象原型立链的终点
2.undefined表示缺少值   ，此处应该有值，但是没有定义
    (1).变量被声明了，但是没有赋值
    (2)调用函数时，应该提供的参数没有提供
    (3)对象没有赋值的属性
    (4)函数没有返回值时，默认返回undefined